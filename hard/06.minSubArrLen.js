/*
   Task Description:
   Write a function called "minSubArrLen" that takes an array of positive integers and a target number as parameters. The function should return the minimal length of a contiguous subarray of the input array in which the sum of the subarray elements is greater than or equal to the target number. If there isn't such a subarray, the function should return 0.

   Example:
   var result1 = minSubArrLen([2, 3, 1, 2, 4, 3], 7);
   console.log(result1); // Output: 2 (The smallest subarray with a sum greater than or equal to 7 is [4, 3])

   var result2 = minSubArrLen([1, 4, 16, 22, 5, 7, 8, 9, 10], 39);
   console.log(result2); // Output: 3 (The smallest subarray with a sum greater than or equal to 39 is [22, 5, 7])

   var result3 = minSubArrLen([1, 2, 3], 100);
   console.log(result3); // Output: 0 (There is no subarray with a sum greater than or equal to 100)
*/

function minSubArrLen(arr, target) {
    //your code here
}