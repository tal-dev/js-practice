/*
   Task Description:
   Write a function called "largestDiff".
   Given an array of integers, "largestDiff" returns the largest difference between any two elements in the array.

   Example:
   var arr = [2, 3, 8, 1];
   var maxDifference = largestDiff(arr);
   console.log(maxDifference); // --> 7
*/

function largestDiff(array) {
    // your code here
}