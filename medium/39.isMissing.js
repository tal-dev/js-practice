/*
   Task Description:
   Write a function called "isMissing" that takes an unsorted array of unique numbers from 1 through some number n (with either no missing numbers or exactly one missing number) and returns the missing number in the sequence.

   Example:
   var arr1 = [1, 2, 3, 5, 6];
   var result1 = isMissing(arr1);
   console.log(result1); // Output: 4

   var arr2 = [1, 3, 7, 9, 11];
   var result2 = isMissing(arr2);
   console.log(result2); // Output: 5

   var arr3 = [1, 2, 3, 4, 5];
   var result3 = isMissing(arr3);
   console.log(result3); // Output: "no missing"
*/

function isMissing(arr) {
    // your code here
}
  