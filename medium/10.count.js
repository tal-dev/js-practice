/*
   Task Description:
   Write a function called "count" that accepts a string as a parameter and returns the number of non-space characters found in the string.

   Example:
   var text = "Honey pie, you are driving me crazy";
   var characterCount = count(text);
   console.log(characterCount); // --> 29
*/

function count(str) {
    // your code here
}