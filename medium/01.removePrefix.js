/*
   Task Description:
   Write a function called "removePrefix".
   Given an array of strings, "removePrefix" removes the '$' sign from numbers in the strings and returns an array of parsed numbers.

   Example:
   var inputArray = ['$2', '$3', '$4', '$5', '$15'];
   var output = removePrefix(inputArray);
   console.log(output); // --> [2, 3, 4, 5, 15]
*/

function removePrefix(arr) {
    // your code here
}