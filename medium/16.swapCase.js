/*
   Task Description:
   Write a JavaScript function called "swapCase" that accepts a string as a parameter. The function should swap the cases of the letters in the string, converting uppercase letters to lowercase and lowercase letters to uppercase.

   Example:
   var input = 'AaBbc';
   var result = swapCase(input);
   console.log(result); // Output: "aAbBC"
*/

function swapCase(str) {
    // your code here
}