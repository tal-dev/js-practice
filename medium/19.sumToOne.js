/*
   Task Description:
   Write a JavaScript function called "sumToOne" that accepts an integer "num" as a parameter. 
   The function should repeatedly sum the digits of the given integer until the result is a single-digit number. 
   It should then return that single-digit result.

   Example:
   var result = sumToOne(928);
   console.log(result); // Output: 1
   // Explanation: 9 + 2 + 8 = 19, then 1 + 9 = 10, finally 1 + 0 = 1
*/

function sumToOne(num) {
    // your code here
}