/*
   Task Description:
   Write a JavaScript function called "reverse" that accepts a numerical array "arr" as a parameter. 
   The function should reverse the order of values in the array in place, 
   meaning you should not use a second array and should move the values within the given array to reverse their order.

   Example:
   var arr = [1, 2, 3, 4];
   reverse(arr);
   console.log(arr); // Output: [4, 3, 2, 1]
*/


function reverse(arr) {
    // your code here
}