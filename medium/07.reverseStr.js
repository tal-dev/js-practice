/*
   Task Description:
   Write a function called "reverseStr" that accepts a string as a parameter and returns the reverse of that string.

   Example:
   var inputString = 'hello me';
   var reversedString = reverseStr(inputString);
   console.log(reversedString); // --> 'em olleh'
*/

function reverseStr(str) {
    // your code here
}