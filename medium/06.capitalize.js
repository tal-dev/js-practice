/*
   Task Description:
   Write a function called "capitalize" that accepts a string as a parameter and converts the first letter of each word in the string to uppercase.

   Example:
   var inputString = 'the quick brown fox';
   var capitalizedString = capitalize(inputString);
   console.log(capitalizedString); // --> 'The Quick Brown Fox'
*/

function capitalize(str) {
    // your code
}
  