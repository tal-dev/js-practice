/*
   Task Description:
   Write a function called "acronym" that accepts a string as input. It should create an acronym from the input string by taking the first letter of each word and capitalizing it. The acronym should not include punctuation characters.

   Example:
   var inputString1 = "there's no free lunch - gotta pay yer way.";
   var result1 = acronym(inputString1);
   console.log(result1); // Output: "TNFLGPYW"

   var inputString2 = "Live from New York, it's Saturday Night!";
   var result2 = acronym(inputString2);
   console.log(result2); // Output: "LFNYISN"
*/

function acronym(str) {
    // your code here
}