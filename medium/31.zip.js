/*
   Task Description:
   Write a function called "zip" that accepts two arrays. It should combine their values sequentially into a new array, starting with the first array. Extra values from either array should be included afterward.

   Example:
   var arr1 = [1, 2];
   var arr2 = [10, 20, 30, 40];
   var result = zip(arr1, arr2);
   console.log(result); // Output: [1, 10, 2, 20, 30, 40]

   var arr3 = ['a', 'b', 'c'];
   var arr4 = [1, 2, 3, 4, 5];
   var result2 = zip(arr3, arr4);
   console.log(result2); // Output: ['a', 1, 'b', 2, 'c', 3, 4, 5]
*/

function zip(arr1, arr2) {
    // your code here
}