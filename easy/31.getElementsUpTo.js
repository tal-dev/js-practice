/*
   Task Description:
   Write a function called "getElementsUpTo" that takes an array and an index as input.

   "getElementsUpTo" should return a new array containing all the elements up until (but not including) the element at the given index.

   Notes: In order to do this, you should be familiar with the 'splice' method.
*/

function getElementsUpTo(array, n) {
    // your code here
}

