/*
   Task Description:
   Write a function called "addToBack" that takes an array and an element as input.

   "addToBack" should add the given element to the end of the given array and return the SAME array (not a new one).
*/

function addToBack(arr, element) {
    // your code here
}