/*
   Task Description:
   Write a function called "removeArrayValues" that takes an object as input.

   "removeArrayValues" should remove any properties from the object whose values are arrays.

   Example:
   var obj = {
     a: [1, 3, 4],
     b: 2,
     c: ['hi', 'there']
   };
   removeArrayValues(obj);

   After calling removeArrayValues, the object should be modified as follows:
   console.log(obj); // should print { b: 2 }
*/

function removeNumberValues(obj) {
    // your code here
}