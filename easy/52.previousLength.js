/*
   Task Description:
   Write a function called "prevLen" that takes an array of strings as input.

   Within the same array, "prevLen" should replace each string with a number representing the length of the string in the previous array index.

   Return the modified array.

   Example:
   prevLen(['av', 's', 'asdb', 'adfgb']) should return [5, 2, 1, 4].
*/

