/*
   Task Description:
   Write a function called "transformFirstLast".
   Given an array, "transformFirstLast" returns an object with two properties:
   - "first" set to the value of the first element in the array.
   - "last" set to the value of the last element in the array.
   
   Notes:
   - If the array is empty, it should return an empty object ({}).

   Example:
   var arr = [1, 2, 3, 4];
   var output = transformFirstLast(arr);
   console.log(output); // --> { first: 1, last: 4 }
*/

function transformFirstLast(arr) {
    // your code here
}