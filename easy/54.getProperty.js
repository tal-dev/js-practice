/*
   Task Description:
   Write a function called "getProperty" that takes an object and a key as input.

   "getProperty" should return the value of the property at the given key.

   Notes:
   - If there is no property at the given key, it should return undefined.

   Example:
   var obj = {
     key: 'value'
   };
   var output = getProperty(obj, 'key');
   console.log(output); // Output: 'value'
*/

function getProperty(obj, key) {
    // your code here
}