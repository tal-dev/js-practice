/*
   Task Description:
   Write a function called "addToFrontOfNew" that takes an array and an element as input.

   "addToFrontOfNew" should return a new array containing all the elements of the given array, with the given element added to the front.

   Important: It should be a NEW array instance, not the original array instance.
*/

function addToFrontOfNew(arr, element) {
    // your code here
}
