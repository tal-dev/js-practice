/*
   Task Description:
   Create a function that accepts a number as an input.

   The function should return a new array that counts down by one, from the number (as the array's 'zeroth' element) down to 0 (as the last element).

   Additionally, the function should return the length of this array.

   Example:
   countdown(5) // [5,4,3,2,1,0]

*/

function countdown(num) {
    // your code here	
}