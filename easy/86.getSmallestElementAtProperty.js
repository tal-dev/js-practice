/*
   Task Description:
   Write a function called "getSmallestElementAtProperty" that takes an object and a key as input.

   "getSmallestElementAtProperty" should return the smallest element in the array located at the given key.

   Notes:
   - If the array is empty, it should return undefined.
   - If the property at the given key is not an array, it should return undefined.
   - If there is no property at the key, it should return undefined.

   Example:
   var obj = {
     key: [2, 1, 5]
   };
   var output = getSmallestElementAtProperty(obj, 'key');
   console.log(output); // should print 1
*/

function getSmallestElementAtProperty(obj, key) {
    // your code here
}