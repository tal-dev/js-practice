/*
   Task Description:
   Write a function called "addToFront" that takes an array and an element as input.

   "addToFront" should add the given element to the front of the given array and return the SAME array (not a new one).

   Example:
   var output = addToFront([1, 2], 3);
   console.log(output); // -> [3, 1, 2]
*/


function addToFront(arr, element) {
    // your code here
}