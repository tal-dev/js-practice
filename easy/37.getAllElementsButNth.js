/*
   Task Description:
   Write a function called "getAllElementsButNth" that takes an array and an index as input.

   "getAllElementsButNth" should return an array with all the elements of the given array except for the element at the specified index.

   Example:
    var output = getAllElementsButNth(['a', 'b', 'c'], 1);
    console.log(output); // --> ['a', 'c']
*/

function getAllElementsButNth(array, n) {
    // your code here
}