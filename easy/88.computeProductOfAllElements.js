/*
   Task Description:
   Write a function called "computeProductOfAllElements" that takes an array of numbers as input.

   "computeProductOfAllElements" should return the product of all the elements in the given array.

   Notes:
   - If the given array is empty, it should return 0.

   Example:
   var output = computeProductOfAllElements([2, 5, 6]);
   console.log(output); // should print 60
*/

function computeProductOfAllElements(arr) {
    // your code here
}