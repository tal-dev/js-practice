/*
   Task Description:
   Write a function called "findShortestElement" that takes an array of strings as input.

   "findShortestElement" should return the shortest string within the given array.

   Notes:
   - If there are ties (multiple strings with the same shortest length), it returns the first element to appear.
   - If the given array is empty, it should return an empty string.

   Example:
   var output = findShortestElement(['a', 'two', 'three']);
   console.log(output); // should print 'a'
*/

function findShortestElement(arr) {
    // your code here
}