/*
   Task Description:
   Write a function called "replace1" that takes an array, a target item, a starting index, and a new item as input.

   "replace1" should remove 2 items starting from the given index of the target item and add the new item in their place.

   Example 1:
   replace1(["Banana", "Orange", "Apple", "Mango"], "Orange", 2, "Melon") should return ["Banana", "Melon", "Mango"].

   Example 2:
   replace1([5, 1, 0, -4], 1, 2, 9) should return [5, 9, -4].
*/

function replace(arr, item, removeCount, value ) {
    // your code here
}