/*
   Task Description:
   Write a function called "getLongestElement" that takes an array of strings as input.

   "getLongestElement" should return the longest string in the given array.

   Notes:
   - If there are ties (multiple strings with the same longest length), it returns the first element to appear.
   - If the array is empty, it should return an empty string.

   Example:
   var output = getLongestElement(['one', 'two', 'three']);
   console.log(output); // should print 'three'
*/

function getLongestElement(arr) {
    // your code here
}