/*
   Task Description:
   Write a function called "removeProperty" that takes an object and a key as input.

   "removeProperty" should remove the given key from the given object.

   Example:
   var obj = {
     name: 'Sam',
     age: 20
   };
   removeProperty(obj, 'name');
   console.log(obj.name); // Output: undefined
*/

function removeProperty(obj, key) {
    // your code here
}