/*
   Task Description:
   Write a function called "removeOddValues" that takes an object as input.

   "removeOddValues" should remove any properties from the object whose values are odd numbers.

   Example:
   var obj = {
     a: 2,
     b: 3,
     c: 4
   };
   removeOddValues(obj);

   After calling removeOddValues, the object should be modified as follows:
   console.log(obj); // should print { a: 2, c: 4 }
*/

function removeOddValues(obj) {
    // your code here
}