/*
   Task Description:
   Write a function called "addProperty" that takes an object and a key as input.

   "addProperty" should set a new property on the given object with a value of true.

   Example:
   var myObj = {};
   addProperty(myObj, 'myProperty');
   console.log(myObj.myProperty); // Output: true
*/

function addProperty(obj, key) {
    // your code here
}