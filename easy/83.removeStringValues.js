/*
   Task Description:
   Write a function called "removeStringValues" that takes an object as input.

   "removeStringValues" should remove any properties from the object whose values are strings.

   Example:
   var obj = {
     name: 'Sam',
     age: 20
   };
   removeStringValues(obj);

   After calling removeStringValues, the object should be modified as follows:
   console.log(obj); // should print { age: 20 }
*/

function removeStringValues(obj) {
    // your code here
}