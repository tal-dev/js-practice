/*
   Task Description:
   Write a function called "countNumberOfKeys" that takes an object as input.

   "countNumberOfKeys" should return the number of properties the given object has.

   Example:
   var obj = {
     a: 1,
     b: 2,
     c: 3
   };
   var output = countNumberOfKeys(obj);
   console.log(output); // should return 3
*/

function countNumberOfKeys(obj) {
    // your code here
}