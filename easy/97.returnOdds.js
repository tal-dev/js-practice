/*
   Task Description:
   Write a function called "returnOdds" that takes an array of numbers as an argument and returns a new array containing only the odd numbers from the input array.

   Example:
   var arr = [1, 2, 3, 4];
   var result = returnOdds(arr);
   console.log(result); // should return [1, 3]
*/

function returnOdds(arr){
    // ur code  
}
   