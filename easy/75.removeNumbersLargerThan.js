/*
   Task Description:
   Write a function called "removeNumbersLargerThan" that takes a number and an object as input.

   "removeNumbersLargerThan" should remove any properties from the object whose values are numbers greater than the given number.

   Example:
   var obj = {
     a: 8,
     b: 2,
     c: 'montana'
   };
   removeNumbersLargerThan(5, obj);

   After calling removeNumbersLargerThan, the object should be modified as follows:
   console.log(obj); // should print { b: 2, c: 'montana' }
*/

function removeNumbersLargerThan(num, obj) {
    // your code here
}