/*
   Task Description:
   Write a function called "removeElement" that takes an array of elements and a "discarder" parameter as input.

   "removeElement" should return an array containing the items in the given array that do not match the "discarder" parameter.

   Notes:
   - If all the elements match, it should return an empty array.
   - If an empty array is passed in, it should return an empty array.
*/

function removeElement(array, discarder) {
    // your code here
}
