/*
   Task Description:
   Write a function called "isPersonOldEnoughToDrive" that takes a "person" object as input.

   "isPersonOldEnoughToDrive" should return whether the given person is old enough to drive.

   Notes:
   - The legal driving age in the United States is 16.

   Example:
   var obj = {
     age: 16
   };
   var output = isPersonOldEnoughToDrive(obj);
   console.log(output); // Output: true
*/

function isPersonOldEnoughToDrive(person) {
  // your code here
}
