/*
   Task Description:
   Write a function called "getAllWords" that takes a sentence as input.

   "getAllWords" should return an array containing every word in the sentence.

   Notes:
   - If given an empty string, it should return an empty array.
*/

function getAllWords(str) {
    // your code here
}