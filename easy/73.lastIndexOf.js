/*
   Task Description:
   Implement a custom method called "lastIndexOf" for arrays.

   "lastIndexOf" should take an element as input and return the index of the last occurrence of that element in the array.

   Example:
   const students = [1, 2, 3, 4, 2];
   students.lastIndexOf(2); // should return 4.
*/

